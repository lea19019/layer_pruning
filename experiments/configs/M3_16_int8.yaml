base_model: CohereForAI/aya-expanse-8b
description: Heuristic pruning (16 removed) + FT + INT8
distillation:
  enabled: false
experiment_id: M3_16_int8
finetuning:
  enabled: true
  epochs: 3
  qlora: false
pruning:
  method: heuristic
  target_layers: 16
  val_size: 200
quantization:
  bits: 8
  enabled: true
seed: 42
