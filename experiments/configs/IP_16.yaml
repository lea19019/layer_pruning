base_model: CohereForAI/aya-expanse-8b
description: IFR pruning only (-16 layers), no fine-tuning
distillation:
  enabled: false
experiment_id: IP_16
finetuning:
  enabled: false
pruning:
  method: ifr
  n_remove: 16
  scores_path: experiments/results/ifr_scores.json
quantization:
  enabled: false
seed: 42
