base_model: CohereForAI/aya-expanse-8b
description: IFR threshold=0.5 pruning, LoRA FT, (English-Spanish)
distillation:
  enabled: false
experiment_id: I5_t05_ft_enes
finetuning:
  enabled: true
  epochs: 3
  qlora: false
lang_pair: en-es
pruning:
  method: ifr
  scores_path: experiments/results/ifr_scores_enes.json
  threshold_factor: 0.5
quantization:
  enabled: false
seed: 42
