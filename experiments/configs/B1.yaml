# B1: Quantization only
experiment_id: B1
description: "Baseline: Aya Expanse 8B with INT4 quantization only"
base_model: CohereForAI/aya-expanse-8b
seed: 42

pruning:
  method: none

finetuning:
  enabled: false

distillation:
  enabled: false

quantization:
  enabled: true
  quant_type: nf4
