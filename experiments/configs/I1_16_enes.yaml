experiment_id: I1_16_enes
description: IFR pruning (16 removed) + FT (English-Spanish)
base_model: CohereForAI/aya-expanse-8b
seed: 42
lang_pair: en-es
pruning:
  method: ifr
  n_remove: 16
  scores_path: experiments/results/ifr_scores_enes.json
finetuning:
  enabled: true
  epochs: 3
  qlora: false
distillation:
  enabled: false
quantization:
  enabled: false
