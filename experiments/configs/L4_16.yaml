experiment_id: L4_16
description: LRP pruning (16 removed) + FT + KD + INT4
base_model: CohereForAI/aya-expanse-8b
seed: 42
pruning:
  method: lrp
  n_remove: 16
finetuning:
  enabled: true
  epochs: 3
  qlora: false
distillation:
  enabled: true
quantization:
  enabled: true
  quant_type: nf4
