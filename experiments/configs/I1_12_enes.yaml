experiment_id: I1_12_enes
description: IFR pruning (12 removed) + FT (English-Spanish)
base_model: CohereForAI/aya-expanse-8b
seed: 42
lang_pair: en-es
pruning:
  method: ifr
  n_remove: 12
  scores_path: experiments/results/ifr_scores_enes.json
finetuning:
  enabled: true
  epochs: 3
  qlora: false
distillation:
  enabled: false
quantization:
  enabled: false
