experiment_id: M1_8
description: Heuristic pruning (8 removed -> 24 layers) + fine-tuning
base_model: CohereForAI/aya-expanse-8b
seed: 42
pruning:
  method: heuristic
  target_layers: 24
  val_size: 50
finetuning:
  enabled: true
  epochs: 3
  qlora: false
distillation:
  enabled: false
quantization:
  enabled: false
