experiment_id: L3_8
description: LRP pruning (8 removed) + FT + INT4
base_model: CohereForAI/aya-expanse-8b
seed: 42
pruning:
  method: lrp
  n_remove: 8
finetuning:
  enabled: true
  epochs: 3
  qlora: false
distillation:
  enabled: false
quantization:
  enabled: true
  quant_type: nf4
