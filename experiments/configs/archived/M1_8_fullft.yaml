experiment_id: M1_8_fullft
description: Heuristic pruning (8 removed) + full fine-tuning (Moslem replication)
base_model: CohereForAI/aya-expanse-8b
seed: 42
pruning:
  method: heuristic
  target_layers: 24
  val_size: 200
finetuning:
  enabled: true
  full_ft: true
  epochs: 1
distillation:
  enabled: false
quantization:
  enabled: false
