experiment_id: M5_16
description: Iterative pruning (16 removed) + full FT (Moslem replication)
base_model: CohereForAI/aya-expanse-8b
seed: 42
pruning:
  method: heuristic
  target_layers: 16
  val_size: 200
finetuning:
  enabled: true
  full_ft: true
  epochs: 1
distillation:
  enabled: false
quantization:
  enabled: false
