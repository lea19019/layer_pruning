base_model: CohereForAI/aya-expanse-8b
description: IFR pruning (12 removed) + FT + KD + INT8 (English-Spanish)
distillation:
  enabled: true
experiment_id: I4_12_int8_enes
finetuning:
  enabled: true
  epochs: 3
  qlora: false
lang_pair: en-es
pruning:
  method: ifr
  n_remove: 12
  scores_path: experiments/results/ifr_scores_enes.json
quantization:
  bits: 8
  enabled: true
seed: 42
