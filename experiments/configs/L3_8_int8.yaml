base_model: CohereForAI/aya-expanse-8b
description: LRP pruning (8 removed) + FT + INT8
distillation:
  enabled: false
experiment_id: L3_8_int8
finetuning:
  enabled: true
  epochs: 3
  qlora: false
pruning:
  method: lrp
  n_remove: 8
quantization:
  bits: 8
  enabled: true
seed: 42
