base_model: CohereForAI/aya-expanse-8b
description: IFR threshold=0.5 pruning, LoRA FT, KD from Aya 32B, (English-Spanish)
distillation:
  enabled: true
experiment_id: I5_t05_kd_enes
finetuning:
  enabled: true
  epochs: 3
  qlora: false
lang_pair: en-es
pruning:
  method: ifr
  scores_path: experiments/results/ifr_scores_enes.json
  threshold_factor: 0.5
quantization:
  enabled: false
seed: 42
