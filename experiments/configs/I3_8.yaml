experiment_id: I3_8
description: IFR pruning (8 removed) + FT + INT4
base_model: CohereForAI/aya-expanse-8b
seed: 42
pruning:
  method: ifr
  n_remove: 8
  scores_path: experiments/results/ifr_scores.json
finetuning:
  enabled: true
  epochs: 3
  qlora: false
distillation:
  enabled: false
quantization:
  enabled: true
  quant_type: nf4
