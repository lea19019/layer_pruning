base_model: CohereForAI/aya-expanse-8b
description: Heuristic pruning (16 removed) + FT + KD (English-Spanish)
distillation:
  enabled: true
experiment_id: M2_16_enes
finetuning:
  enabled: true
  epochs: 3
  qlora: false
lang_pair: en-es
pruning:
  method: heuristic
  target_layers: 16
  val_size: 200
quantization:
  enabled: false
seed: 42
