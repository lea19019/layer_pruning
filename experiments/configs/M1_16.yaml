experiment_id: M1_16
description: Heuristic pruning (16 removed -> 16 layers) + fine-tuning
base_model: CohereForAI/aya-expanse-8b
seed: 42
pruning:
  method: heuristic
  target_layers: 16
  val_size: 50
finetuning:
  enabled: true
  epochs: 3
  qlora: false
distillation:
  enabled: false
quantization:
  enabled: false
