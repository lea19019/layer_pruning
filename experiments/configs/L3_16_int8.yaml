base_model: CohereForAI/aya-expanse-8b
description: LRP pruning (16 removed) + FT + INT8
distillation:
  enabled: false
experiment_id: L3_16_int8
finetuning:
  enabled: true
  epochs: 3
  qlora: false
pruning:
  method: lrp
  n_remove: 16
quantization:
  bits: 8
  enabled: true
seed: 42
