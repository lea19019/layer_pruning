base_model: CohereForAI/aya-expanse-8b
description: LRP pruning (8 removed) + FT + KD + INT8
distillation:
  enabled: true
experiment_id: L4_8_int8
finetuning:
  enabled: true
  epochs: 3
  qlora: false
pruning:
  method: lrp
  n_remove: 8
quantization:
  bits: 8
  enabled: true
seed: 42
