base_model: CohereForAI/aya-expanse-8b
description: Heuristic pruning (12 removed) + FT + INT8 (English-Spanish)
distillation:
  enabled: false
experiment_id: M3_12_int8_enes
finetuning:
  enabled: true
  epochs: 3
  qlora: false
lang_pair: en-es
pruning:
  method: heuristic
  target_layers: 20
  val_size: 200
quantization:
  bits: 8
  enabled: true
seed: 42
